# Text-to-Video Generator

Автоматизированная система для создания видеороликов из текстовых сценариев с использованием Microsoft Edge TTS, автоматических субтитров и фоновой музыки.

## 🎯 Возможности

- ✅ **Генерация речи через Edge TTS** - 400+ натуральных голосов на 100+ языках
- ✅ **Высокое качество озвучки** с нейронными голосами Microsoft
- ✅ **Автоматическая синхронизация субтитров** с точными таймингами
- ✅ **Поддержка различных форматов видео**: 16:9, 9:16, 1:1, 4:3, 21:9
- ✅ **Фоновая музыка** с регулировкой громкости и fade-эффектами
- ✅ **Настройка через YAML** конфигурацию
- ✅ **Мощный CLI интерфейс** с гибкими параметрами
- ✅ **Многоязычность**: Русский, Английский и другие языки
- ✅ **Система логирования** для отладки
- ✅ **Обработка ошибок** с информативными сообщениями
- ✅ **245+ тестов** с 98% покрытием кода

## 🚀 Быстрый старт

### Требования

- Python 3.9+
- FFmpeg (установлен и доступен в PATH)
- **Интернет-соединение** (для Edge TTS)
- 2-4 GB RAM

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd text2video

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt

# Проверить установку FFmpeg
ffmpeg -version
```

### Использование

```bash
# Базовое использование
python main.py --input script.txt --output video.mp4

# С кастомной конфигурацией
python main.py -i script.txt -o video.mp4 -c config.yaml

# Указать формат видео
python main.py -i script.txt --aspect-ratio 9:16

# Без фоновой музыки
python main.py -i script.txt --no-music

# Выбрать язык озвучки
python main.py -i script.txt --language en

# Выбрать голос для озвучки
python main.py -i script.txt --voice en-US-JennyNeural

# Использовать женский русский голос
python main.py -i script.txt --voice ru-RU-SvetlanaNeural

# Использовать изображения по порядку (по именам файлов)
python main.py -i script.txt --image-mode ordered

# Показать версию
python main.py --version

# Минимальный вывод
python main.py -i script.txt --quiet

# Подробное логирование
python main.py -i script.txt --log-level DEBUG
```

### Все доступные параметры

| Параметр | Короткий | Описание | Значение по умолчанию |
|----------|----------|----------|------------------------|
| `--input` | `-i` | Путь к текстовому файлу (.txt или .md) | **Обязательный** |
| `--output` | `-o` | Путь для сохранения видео | `output/video_TIMESTAMP.mp4` |
| `--config` | `-c` | Путь к файлу конфигурации | `config.yaml` |
| `--aspect-ratio` | - | Соотношение сторон видео | `16:9` |
| `--resolution` | - | Разрешение видео (ШxВ) | `1920x1080` |
| `--language` | - | Язык озвучки (ru/en) | `ru` |
| `--voice` | - | Имя голоса Edge TTS (например: ru-RU-DmitryNeural) | `ru-RU-DmitryNeural` |
| `--image-mode` | - | Режим выбора изображений (random/ordered) | `random` |
| `--music-volume` | - | Громкость музыки (0.0-1.0) | `0.2` |
| `--no-music` | - | Отключить фоновую музыку | `false` |
| `--no-subtitles` | - | Отключить субтитры | `false` |
| `--log-level` | - | Уровень логирования | `INFO` |
| `--quiet` | - | Минимальный вывод | `false` |
| `--version` | - | Показать версию | - |
| `--help` | `-h` | Показать справку | - |

## 📁 Структура проекта

```
text2video/
├── main.py                 # Точка входа
├── config.yaml             # Конфигурация
├── requirements.txt        # Зависимости
├── modules/                # Основные модули
│   ├── scene_parser.py     # Парсинг текста
│   ├── tts_generator.py    # Генерация речи
│   ├── subtitle_generator.py # Субтитры
│   ├── visual_selector.py  # Выбор изображений
│   ├── music_selector.py   # Выбор музыки
│   └── video_assembler.py  # Сборка видео
├── utils/                  # Утилиты
├── assets/                 # Медиа-ресурсы
│   ├── images/            # Фоновые изображения
│   ├── music/             # Фоновая музыка
│   └── fonts/             # Шрифты
├── output/                # Сгенерированные видео
├── temp/                  # Временные файлы
└── tests/                 # Тесты

```

## 🎬 Пример сценария

Создайте файл `script.txt`:

```
Добро пожаловать в мир автоматизации создания видео!
Эта система позволяет превратить любой текст в полноценный видеоролик.

Второй абзац становится второй сценой.
Каждая сцена получает свою озвучку и визуальное оформление.

Наслаждайтесь результатом!
```

Каждый абзац, разделённый пустой строкой, становится отдельной сценой в видео.

## ⚙️ Конфигурация

Основные настройки в `config.yaml`:

```yaml
# Настройки TTS
tts:
  language: "ru"  # ru | en
  speed: 1.0

# Настройки видео
video:
  fps: 30
  aspect_ratio: "16:9"  # 16:9 | 9:16 | 1:1
  resolution:
    width: 1920
    height: 1080

# Настройки субтитров
subtitles:
  enabled: true
  font_size: 48
  color: "white"
  max_chars_per_line: 40

# Настройки аудио
audio:
  music:
    enabled: true
    volume: 0.2  # Громкость относительно речи
```

## 🎙️ Выбор голоса (Edge TTS)

Система использует Microsoft Edge TTS с **400+ натуральными голосами** на 100+ языках.

### Популярные голоса

#### Русские голоса:
- **ru-RU-DmitryNeural** (мужской) - стандартный, четкий голос
- **ru-RU-SvetlanaNeural** (женский) - мягкий, естественный
- **ru-RU-DariyaNeural** (женский) - яркий, четкий

#### Английские голоса:
- **en-US-GuyNeural** (мужской) - дружелюбный, легкий
- **en-US-JennyNeural** (женский) - приятный, естественный
- **en-GB-RyanNeural** (британский мужской) - яркий, выразительный

### Использование

#### Через CLI:
```bash
# Мужской русский голос
python main.py -i story.txt --voice ru-RU-DmitryNeural

# Женский русский голос
python main.py -i tutorial.txt --voice ru-RU-SvetlanaNeural

# Женский английский голос
python main.py -i script_en.txt --voice en-US-JennyNeural

# Просмотреть все доступные голоса
edge-tts --list-voices | grep ru-RU
```

#### Через config.yaml:
```yaml
tts:
  language: "ru"
  voice: "ru-RU-DmitryNeural"
  speed: 1.7  # Скорость речи (0.5 - 2.0)
```

### Просмотр всех голосов

Чтобы увидеть полный список доступных голосов:

```bash
# Все голоса
edge-tts --list-voices

# Только русские
edge-tts --list-voices | grep ru-RU

# Только английские
edge-tts --list-voices | grep en-US
```

Полный список голосов также доступен в [документации Microsoft](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/language-support)

## 🖼️ Управление порядком изображений

Система поддерживает два режима выбора изображений для сцен:

### Режим Random (по умолчанию)
Изображения выбираются случайным образом из директории `assets/images/` для каждой сцены.

### Режим Ordered (упорядоченный)
Изображения выбираются по порядку на основе **номера сцены в имени файла**.

#### Поддерживаемые форматы имен файлов:

**Базовые форматы (номер сцены):**
- `1.jpg`, `2.jpg`, `3.jpg` - просто номер
- `scene-1.jpg`, `scene-2.jpg` - с префиксом "scene"

**С указанием длительности:**
- `1_5.jpg` - сцена 1, длительность 5 секунд
- `2_10.5.jpg` - сцена 2, длительность 10.5 секунд
- `scene-3_7.jpg` - сцена 3, длительность 7 секунд

#### Примеры использования:

```bash
# Через CLI
python main.py -i script.txt --image-mode ordered

# Через config.yaml
visuals:
  selection_mode: "ordered"
```

#### Как это работает:

1. **Номер сцены**: Система извлекает номер сцены из имени файла
2. **Сопоставление**: Изображение с номером N используется для сцены N
3. **Длительность** (опционально): Если указана в имени файла (например, `_5` для 5 секунд), она **переопределяет** длительность аудио для этой сцены

#### Пример структуры:

```
assets/images/
├── 1_3.jpg          # Сцена 1, показывается 3 секунды (независимо от аудио)
├── 2.jpg            # Сцена 2, длительность по аудио
├── scene-3_10.jpg   # Сцена 3, показывается 10 секунд
└── 4_5.5.jpg        # Сцена 4, показывается 5.5 секунд
```

#### Важные замечания:

- В ordered режиме **количество изображений должно соответствовать количеству сцен**
- Если изображение для сцены не найдено, будет ошибка с указанием недостающего номера
- Длительность из имени файла **переопределяет** длительность аудио
- Это полезно для создания пауз, замедления или ускорения показа определенных сцен

## 🧪 Разработка

```bash
# Установить dev зависимости
pip install -r requirements-dev.txt

# Запустить тесты
pytest

# Запустить тесты с coverage
pytest --cov=modules --cov=utils tests/

# Форматирование кода
black .

# Проверка кода
flake8 .
```

## 📚 Документация

Подробная документация доступна в:
- [Архитектура](docs/architecture.md)
- [Полная документация](docs/docs.md)
- [План разработки](docs/plan.md)
- [CLAUDE.md](CLAUDE.md) - гайд для AI ассистентов

## 🤝 Участие в проекте

Pull requests приветствуются! Для крупных изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

MIT License

## 🐛 Устранение неполадок

### FFmpeg не найден
```bash
# macOS
brew install ffmpeg

# Ubuntu/Debian
sudo apt-get install ffmpeg

# Windows
# Скачать с https://ffmpeg.org/download.html и добавить в PATH
```

### Ошибка подключения к Edge TTS
- Проверьте интернет-соединение
- Убедитесь, что не используется прокси или VPN, блокирующий Microsoft
- Попробуйте другую сеть

### Недостаточно памяти
- Уменьшите разрешение видео в config.yaml
- Разбейте длинный текст на несколько файлов
- Закройте другие приложения

### Проблемы с субтитрами
- Проверьте, что аудио корректно сгенерировано
- Убедитесь, что `duration` сцены установлена
- Используйте `--log-level DEBUG` для диагностики

## 📊 Технические характеристики

- **Языки программирования**: Python 3.11
- **TTS**: Microsoft Edge TTS (400+ голосов)
- **Модулей**: 6 основных + 2 утилитарных
- **Тестов**: 245+ (243 проходят успешно)
- **Покрытие кода**: ~98%
- **Строк кода**: ~3400
- **Поддержка**: macOS, Linux, Windows

## 🛠️ Статус разработки

**Текущий статус:** ✅ MVP завершен (92%)

### Завершенные этапы:
- ✅ Этап 1-11: Все основные модули реализованы
- ✅ CLI интерфейс с 14 параметрами
- ✅ Система логирования
- ✅ Обработка ошибок с custom exceptions
- ✅ 247 автоматических тестов

### Следующие шаги:
- 🔄 Этап 12: Финальная полировка MVP
- ⏳ Этап 13+: Расширения (кэширование, Ken Burns эффект, web UI)

Отслеживайте прогресс в [TASKS.md](TASKS.md)
